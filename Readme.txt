En la carpeta compartida por defecto tienes que crear los 2 ficheros de 
configuracion en este caso "sitio1.conf" y "sitio2.conf" en una carpta config por ejemplo.
Y hacemos lo mismo en una carpeta html creamos dos directorios dentro de html.
sitio1 y sitio2 y dentro ponemos los .html. Despues ejecutamos el script de provision.
Y funcionaria todo correctamente si las rutas estan todas bien.
